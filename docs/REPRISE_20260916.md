# Reprise — 16/09/2026, fin de journée

## Où on en est (mesuré, pas relu)
- **Prod** : profil `cloud` (Pro → Sonnet + Marcus Opus, Free → Nemotron via tunnel 18084), backend/worker actifs, timeouts d'origine (a820a3d). Spark : Nemotron servi sur 8001, Gemma 8090, noyau 7.0.0-1019, pilote 580.178.04.
- **Sites** : `digital-humans.fr` sert encore **Entracte** (`noindex`). Le vrai site est prêt sur `/apercu-recent/` : mentions légales + CGV avec clauses IA, SIRET, TVA HT, six SDS liées dans « The Work ». DEOS et SH Conseil ont leurs pages `/legal` et `/privacy`. Hygiène faite (vhosts `.pre-*`, `/var/www/digital-humans.fr`, `/test-site/`).
- **Attend Sam** : relecture juridique (ce soir), numéro de TVA (« en cours » sinon), puis **bascule** = C.9 irréversible + polices auto-hébergées (GL-04).
- **Backlog unique** : `docs/BACKLOG_GOLIVE_2026-10-01.md` — 41 lignes, 4 bloquants (GL-01 smoke Free+Pro, GL-02 TVA, GL-11 fait, GL-19 mentions IA app), tri des 3 sources validé par Sam, plan à 15 jours §4.
- **Comité** : cron suspendu ; 47 tâches archivées ; 3 `a_faire` restantes.
- **Calibration** : 4 SDS publiées (#176-179), coûts et mesures dans `/root/workspace/site-work/sds-calibration/README.md`.

## Prochaines commandes
1. Vague 0 — Claude Code, dans ce dépôt, branche `claude/vague-c-20260906` :
   « Lis et exécute `docs/missions/VAGUE0_AS02_ENVIRONNEMENT_DE_CORRECTION_SUR.md`. »
   Relecture de la PR : vérifier que les tests rouges ont été rouges (commits « test rouge » avant « vert »), les deux contrôles négatifs, la mesure avant/après de la suite.
2. Après fusion de la vague 0 : quatre Claude Code en parallèle, un par fichier `docs/missions/VAGUE1_*.md`, branches `claude/vague1-{a,b,c,d}`.
3. Bascule du site sur « bascule » de Sam : checklist dans le backlog C.9 + GL-04.

## Décisions de Sam à dater
Canal support ✅ Email-to-Case + alias support@ · carte bancaire sur le Free (avant 22/09) · 3 contenus à publier (avant 23/09) · modèle Pro D2 (avant 26/09) · Trust Center Hostinger · Crédit Logement · 30 contacts Growth.

## Sauvegardes du jour
`/root/workspace/site-work/bascule/` (vhosts, reliquats www), `/root/workspace/site-work/sds-calibration/` (env, yaml, worker.py d'avant, clé ModelArk 600, relais nginx désactivé), `/var/www/dh-preview/apercu-recent/index.html.pre-*` (chaque version du bundle).

---

# Mise à jour — 16/09 fin de journée : vague 1 fusionnée

## Fait aujourd'hui
- **Vague 0 (AS-02)** fusionnée : suite hermétique, rôle `dh_test` (CREATEDB), DSN dans `/root/.dh_test_db.env`.
- **Vague 1 fusionnée** dans `claude/vague-c-20260906` (poussée, 78 commits) : B → C → A → D, aucun conflit git.
  Migrations relinéarisées 016-018 (B), 019-020 (C), 021-022 (A) — **une seule tête `022_sec12_chat_logs_reclamation`**.
  Suite complète sur la fusion : **1 051 passés, 32 rouges** dont les 31 préexistants ; le 32e (grep `shell=True`)
  venait des 21 fichiers `*.pre-*`/`*.bak` non suivis dans `backend/app` — test restreint aux `.py`, corrigé (8e25465).
  Repère avant fusion : tag `avant-fusion-vague1`.
- Deux bloquants découverts et vérifiés par moi : **GL-20** webhook Stripe inopérant avec le SDK 15.1.0
  (`AttributeError` sur tout événement réel → le parcours Pro ne pouvait pas fonctionner) ; **GL-21** aucun des trois
  modèles servis (`claude-opus-5`, `claude-sonnet-5`, `claude-haiku-4-5-20251001`) n'avait de tarif.
- Décisions de Sam appliquées : recueil de consentement Opus (GL-22, à faire) ; rétention 90 jours sauf besoin
  de fonctionnement ; **page de tarifs corrigée et publiée** (Free = « Modèle Nemotron, hébergé en Europe »).

## Demain matin, dans l'ordre
1. **Déployer `vague-c` sur le service de prod** (Sam présent) : sauvegarde base, `alembic upgrade head` (014 → 022),
   redémarrage backend + worker, smoke test (6 assertions, dont build_sds).
2. **Rejouer une souscription Pro de bout en bout en sandbox Stripe** (clés `sk_test_` vérifiées le 16/09) :
   checkout → webhook → palier ouvert → crédits posés. C'est la preuve qui manque pour GL-20.
3. Vérifier que `model_pricing` couvre exactement les `model_id` de `llm_routing.yaml` (GL-21).
4. Smoke Free (Nemotron) + Pro (Sonnet) sur la prod → ferme GL-01.
5. Puis **bascule du site** si la relecture juridique est faite, et vague 2 (23-26/09).

## Toujours en attente de Sam
TVA (« en cours » sinon) · relecture juridique · carte bancaire sur le Free (avant 22/09, conditionne AS-07) ·
3 contenus à publier (avant 23/09) · modèle Pro D2 (avant 26/09) · Trust Center Hostinger · 30 contacts Growth.
